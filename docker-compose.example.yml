services:
  satellite-server:
    image: ghcr.io/bfpimentel/satellite:latest
    container_name: satellite-server
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - ./data:/app/data
